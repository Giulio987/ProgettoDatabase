PRIMA QUERY:
  INTERNA:
  LIBRI_SCRITTI <- NOME_LINGUA γ COUNT ID_AUT (NOME_LINUGA)  //LIBRI SCRITTI PER OGNI AUTORE
  TMP <- π NOME_LINGUA, NUMERO_LIBRI (E_SCRITTO)
  TMPORDERED <- τ NUMERO_LIBRI DESC (TMP)
  RES <- σ ROWNUM() > 0 and ROWNUM() ≤ 5 (TMPORDERED)


SECONDA QUERY:
  INTERNA:
  TMP <- AUTORE ⨝ (AUTORE.ID_AUT = SCRIVE.ID_AUT) SCRIVE
  LIBRI_SCRITTI <- ID_AUT γ COUNT ID_AUT (TMP)                   //LIBRI SCRITTI PER OGNI AUTORE
  RES <- π ID_AUT, NOME_A, COGNOME_A, LIBRI_SCRITTI (TMP)
  MAX_VALUE <- γ MAX(LIBRI_SCRITTI)
  ESTERNA:
  RESFINALE <-  π LIBRI_SCRITTI(σ LIBRI_SCRITTI=MAX_VALUE (RES) )


TERZA QUERY:
  TMP <- EDITORE ⨝ (EDITORE.CODICE = LIBRO.COD_ED) LIBRO
  LIBRI_PUBBLICATI <- COD_ED γ COUNT COD_ED (TMP)                   //LIBRI SCRITTI PER OGNI AUTORE
  RES <- π NOME, LIBRI_PUBBLICATI (TMP)
  MAX_VALUE <- γ MAX(LIBRI_PUBBLICATI)
  ESTERNA:
  RESFINALE <-  π LIBRI_PUBBLICATI(σ LIBRI_PUBBLICATI=MAX_VALUE (RES) )




QUERY AGGIUNTIVE
